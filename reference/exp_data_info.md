# Get experience period of `exp_data`

`exp_start` gets the start of the overall experience period (inclusive).

`exp_end` gets the end of the overall experience period (exclusive).

## Usage

``` r
exp_start(exp_data)

exp_end(exp_data)
```

## Arguments

- exp_data:

  The mortality experience data.

## Value

A scalar `datey`.
