# SIMD active lanes and tier

SIMD active lanes and tier

## Usage

``` r
vec_active_lanes()

vec_active_tier()
```

## Value

A scalar `integer` representing the number of active SIMD lanes.

A scalar `character` describing the active SIMD tier.
