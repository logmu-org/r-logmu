% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{exp_and_val_data}
\alias{exp_and_val_data}
\alias{exp_data}
\alias{val_data}
\title{Create an \code{exp_data} or a \code{val_data}}
\usage{
exp_data(
  columns,
  exp_start,
  exp_end,
  on_unreadable = c("error", "warn", "ignore")
)

val_data(columns, as_at, on_unreadable = c("error", "warn", "ignore"))
}
\arguments{
\item{columns}{The list of columns (which includes things like \code{data.frame}
and \code{tibble}) to convert to an \code{exp_data} or \code{val_data}.}

\item{exp_start}{The start of the overall experience period (inclusive).
Must be before \code{exp_end}.}

\item{exp_end}{The end of the overall experience period (exclusive).
Must be after \code{exp_start}.}

\item{on_unreadable}{What to do about columns whose type \strong{logmu} cannot
read (anything other than \code{double}, \code{integer}, \code{logical}, \code{character},
\code{factor}, \code{datey} or \code{durationy}): \code{"error"} (the default) rejects them,
\code{"warn"} keeps them with a warning, and \code{"ignore"} keeps them silently. The
special columns (\code{birth}, the \verb{E2R_*} columns, \code{count}) are always checked.}

\item{as_at}{The 'as at' date of the valuation data.}
}
\value{
An \code{exp_data} or \code{val_data}.
}
\description{
Create a experience or valuation data from a list of columns, a \code{data.frame} or
\code{tibble} (or similar).

The columns must be atomic, the same length and uniquely named.

There \emph{must} be a \code{birth} date column.

A \code{count} column is optional. If present it must be numeric, non-negative
and finite. Rows with a nil count are dropped.

Experience data must also contain (and valuation data must \emph{not} contain)
\itemize{
\item \code{E2R_start} and \code{E2R_end} date columns, and
\item an \code{E2R_died} logical column.
}

These special columns cannot contain NA and, if they are dates, they must be
\code{datey}.

The experience data period and individual E2Rs must be \emph{proper}:
\itemize{
\item at the dataset level,  \code{exp_start} cannot be after \code{exp_end}, and
\item at the record level, \code{E2R_start} cannot be after \code{E2R_end} and if they are
the same then \code{E2R_died} must be false.
}

The experience data is trimmed to the experience period, i.e.
it is guaranteed that
\code{E2R_start} is not before \code{exp_start} and \code{E2R_end} is not after \code{exp_end}.
Records with empty E2Rs after trimming are dropped.
}
