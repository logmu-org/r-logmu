% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aev.R
\name{as.data.frame.aev}
\alias{as.data.frame.aev}
\title{Coerce an \code{aev} to a data frame}
\usage{
\method{as.data.frame}{aev}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{An \code{aev} object.}

\item{row.names}{Row names for the result, or \code{NULL} for the default.}

\item{optional}{Ignored. Present because the generic has it; every column
name here is already syntactic.}

\item{...}{Ignored.}
}
\value{
A \code{data.frame} with one row per record of \code{x}.
}
\description{
One row per record, carrying the labels, the raw triple and the three
calculated properties a chart reads. This is the frame \strong{logmu}'s own
plotting is built on, and the way to take an \code{aev} into \strong{ggplot2},
\strong{dplyr} or anything else that works on data frames.
}
\section{Columns}{
\tabular{ll}{
   Column \tab Contents \cr
   \code{name} \tab the element label, \code{names()} on the \code{aev} \cr
   \code{group} \tab the group label, \code{\link[=group_names]{group_names()}} on the \code{aev} \cr
   \code{A}, \code{E}, \code{V} \tab the triple itself \cr
   \code{A_over_E} \tab \eqn{A / E} \cr
   \code{log_A_over_E_stddev} \tab \eqn{\sqrt{V} / E} \cr
   \code{deviance_residual} \tab see \link{aev_properties} \cr
}


The calculated columns are named after the properties that produce them, so
\code{frame$A_over_E} and \code{aev$A_over_E} are the same word for the same quantity.
The five remaining properties are left out because each is a line of
arithmetic on \code{A}, \code{E} and \code{V}, and naming them here would fix five more
column names for no gain.

\code{name} and \code{group} are always present, and are \code{NA} on an \code{aev} that carries
no labels. The columns therefore depend on the input's type and never on its
values, which is what lets frames from a broken-down \code{aev} and an ungrouped
one be stacked with \code{rbind()}.
}

\examples{
aev <- create_aev(A = c(1100, 40), E = c(1000, 50), V = c(2500, 125))
names(aev) <- c("65-70", "70-75")
group_names(aev) <- "age"

as.data.frame(aev)

# An unlabelled `aev` gives the same columns, with the labels NA.
as.data.frame(create_aev(A = 1100, E = 1000, V = 2500))
}
