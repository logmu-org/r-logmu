% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model}
\alias{model}
\alias{is_model}
\alias{print.model}
\title{A proportional hazards mortality model}
\usage{
model(ref_mortality, covariates)

is_model(x)

\method{print}{model}(x, ...)
}
\arguments{
\item{ref_mortality}{The reference mortality: a \code{mortality}, or a pronoun
expression for \eqn{\log\mu^{\mathrm{ref}}}. Anything held fixed goes here
-- a reference mortality is already an offset, so fixed effects need no
feature of their own.}

\item{covariates}{A \code{\link[=covariates]{covariates()}} object giving the \eqn{X} terms. The
coefficients belong to the fit; nothing here names or supplies one.}

\item{x}{An object.}

\item{...}{Ignored.}
}
\value{
\code{model()} returns a \code{model}.

\code{is_model()} returns a scalar \code{logical}.
}
\description{
A \code{model} is the right-hand side of
\deqn{\log\mu = \log\mu^{\mathrm{ref}} + \sum_j \beta_j X^{(j)},}
that is a reference mortality and the covariates whose coefficients
\code{\link[=fit]{fit()}} estimates. It carries nothing else: the experience data, the
\code{include}, the weight and the tolerance all belong to the fit, because every
candidate model in a selection exercise shares them.

A fitted model is itself a \code{mortality}, so it can be handed back as the
\code{ref_mortality} of another model.
}
\examples{
age_shape <- variable(.x)
model(
  ref_mortality = gompertz_mortality(),
  covariates    = covariates(male = .i$male, female = !.i$male) * age_shape
)
}
