% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit}
\alias{fit}
\alias{is_fit}
\alias{coef.fit}
\alias{vcov.fit}
\alias{print.fit}
\title{Fit a mortality model by maximum weighted log-likelihood}
\usage{
fit(
  exp_data,
  model,
  include = NULL,
  weight = NULL,
  val_similarity = NULL,
  val_distance = NULL,
  test_mortality = default_mortality(),
  Z = NULL,
  settings = NULL,
  overdispersion = NULL,
  time_scale = NULL,
  start = NULL,
  L_tolerance = default_L_tolerance,
  armijo = default_armijo,
  max_iterations = default_max_iterations,
  max_halvings = default_max_halvings,
  threads = cpp_veil_default_threads()
)

is_fit(x)

\method{coef}{fit}(object, ...)

\method{vcov}{fit}(object, ...)

\method{print}{fit}(x, ...)
}
\arguments{
\item{exp_data}{The experience data.}

\item{model}{The \code{\link[=model]{model()}} to fit.}

\item{include}{An \code{include} restricting the population, or \code{NULL} for all of
it.}

\item{weight}{A pronoun expression for the weight \eqn{w}, or \code{NULL} for
lives. Weights must not be negative.}

\item{val_similarity, val_distance}{A pronoun expression for the second
weighting factor, in either spelling. At most one may be given.}

\item{test_mortality}{The mortality on which the \eqn{Z} scale is measured.
See Details.}

\item{Z}{The \eqn{Z} scale as a single positive number, given instead
of a \code{test_mortality} rather than as well as one.}

\item{settings}{A \code{\link[=settings]{settings()}} object carrying \code{overdispersion} and
\code{time_scale}.}

\item{overdispersion, time_scale}{Given directly, these override the
\code{settings}.}

\item{start}{Starting coefficients, one per model term. Defaults to zeros.}

\item{L_tolerance}{The convergence tolerance, as a gain in \eqn{L}. One unit of
\eqn{L} is about what one parameter costs.}

\item{armijo}{The fraction of the linearly predicted gain a damped step must
actually deliver to be accepted. Must lie strictly between 0 and 0.5.}

\item{max_iterations}{The most Newton steps to take.}

\item{max_halvings}{The most times one step may be halved before the fit is
abandoned.}

\item{threads}{The number of worker threads.}

\item{x, object}{A \code{fit}.}

\item{...}{Ignored.}
}
\value{
\code{fit()} returns a \code{fit}, whose fields are \code{beta}, \code{mortality} (the fitted
model as a \code{mortality}), \code{variance}, \code{log_likelihood}, \code{penalty},
\code{penalised_log_likelihood}, \code{Z}, \code{overdispersion}, \code{diagnostics} (the
population and test-mortality numbers \code{print()} reports), \code{labels} (what was
written for the include, the weight and the test mortality), \code{iterations},
\code{evaluations}, \code{predicted_gain} and \code{model}.

\code{is_fit()} returns a scalar \code{logical}.

\code{coef()} returns the fitted coefficients as a named numeric vector, and
\code{vcov()} their variance-covariance matrix.
}
\description{
Estimates the coefficients of a \code{\link[=model]{model()}} against experience data, by
Newton-Raphson on the weighted log-likelihood
\deqn{L^* = \Omega^{-1}(\mathrm{A}w\log\mu - \mathrm{E}w).}

A fit returns a fit or it fails. A model that does not converge, or whose
information matrix cannot be factored, raises an error carrying the
diagnosis rather than returning a number nobody should use.
}
\section{The Z scale}{
Results are reported on the \eqn{L = Z^{-1}L^*} scale, where
\eqn{Z = \mathrm{E}w^2/\mathrm{E}w} on a test mortality. \eqn{Z} is a change
of units, chosen so that one parameter costs about one unit of \eqn{L}, and
it must be the same for every model being compared -- otherwise the
penalised log-likelihoods are not on one scale and the comparison means
nothing.

It is set in one of two ways, never from the model's own reference mortality,
and they are alternatives rather than a pair:
\itemize{
\item \code{test_mortality} names the mortality \eqn{Z} is measured on, over this same
data, weight and include. It defaults to \code{\link[=default_mortality]{default_mortality()}}, and a fixed
shared choice is worth more here than a good one, since \eqn{Z}'s job is to
be one yardstick.
\item \code{Z} gives \eqn{Z} directly as a number, skipping the measurement.
This is what model selection needs, the caller having obtained it once and
passed it to every candidate, and \code{\link[=compare_models]{compare_models()}} does exactly that.
}

Where the weight is absent, is the literal 1, or is an expression that can be
shown to be an indicator, \eqn{w^2 = w} makes \eqn{Z} exactly one. That is
settled without looking at the data and without consulting \code{test_mortality}
at all, so the unweighted case costs nothing.

Being \emph{shown} to be an indicator is structural, because a column's type is
unknown until the data arrives. \code{!.i$male} and \code{.i$group == "a"} qualify
because the operator forces the type; the bare column \code{.i$male} does not, and
costs one pass over the data to establish a \eqn{Z} of 1.
}

\section{Convergence}{
The iteration stops when the gain in \eqn{L} still available falls below
\code{L_tolerance}, which is a forward-looking test: it measures the distance left to
the optimum rather than the size of the last step, so it is not fooled by a
damped step or by a model whose maximum does not exist. Stopping within
\eqn{\epsilon} of the maximum leaves the coefficients within
\eqn{\sqrt{2\epsilon/I}} of theirs.
}

\examples{
data <- exp_data(
  list(
    birth     = datey::datey(c(1945, 1950, 1955, 1940, 1948)),
    male      = c(TRUE, FALSE, TRUE, FALSE, TRUE),
    E2R_start = datey::datey(rep(2015, 5)),
    E2R_end   = datey::datey(c(2020, 2020, 2018, 2020, 2019)),
    E2R_died  = c(FALSE, FALSE, TRUE, FALSE, TRUE)
  ),
  exp_start = datey::datey(2015),
  exp_end   = datey::datey(2020)
)

m <- model(ref_mortality = gompertz_mortality(), covariates = covariates(level = 1))
fit(data, m, settings = settings(overdispersion = 1))
}
