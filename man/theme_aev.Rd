% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aev_plot.R
\name{theme_aev}
\alias{theme_aev}
\title{The chart theme, and how to resize a chart}
\usage{
theme_aev(base_size = 9)
}
\arguments{
\item{base_size}{Base text size in points. Every other size on the chart is a
multiple of it. The default of 9 is the size the design was drawn at, and
at that size every length is the pixel the prototype specifies.}
}
\value{
A \strong{ggplot2} theme.
}
\description{
The theme \code{\link[=autoplot.aev]{autoplot.aev()}} draws with. Adding it back at a different
\code{base_size} is how a \strong{logmu} chart is resized.
}
\details{
Every length on the chart is a multiple of the base text size -- the marker
and its interval, the off-scale chevrons, the depth of the residual strip,
the group brackets and the legend key. So a chart drawn for a larger page
wants larger type, and gets everything else with it:

\if{html}{\out{<div class="sourceCode">}}\preformatted{autoplot(aev) + theme_aev(base_size = 13)
}\if{html}{\out{</div>}}

A larger device on its own does not do this. It gives the same marker in more
room, which reads thinner and smaller the further it is stretched.

\code{ggplot2::theme(text = ggplot2::element_text(size = 13))} resizes most of the
chart in the same way and is the ordinary \strong{ggplot2} way of asking. It
cannot reach the legend key, which is sized by \code{legend.key.width} and
\code{legend.key.height} and so only by this function. Where the legend matters,
use \code{theme_aev()}.

The result is an ordinary theme and can be extended with \code{+} like any other.
}
\section{Adding a standard theme}{

Replacing this one wholesale -- with \code{ggplot2::theme_minimal()}, say -- still
draws a chart, and rather more of one than you might expect. The two panels,
the group brackets, the off-scale chevrons, the depth of the residual strip
and the sizing all survive, because the facet builds them and not the theme.

What is lost is the styling the design asks for:
\itemize{
\item Grid lines come back in the residual panel, where the prototype
deliberately has none -- the edges of the tint bands do that work, and they
fall in the same places.
\item \code{ggplot2::theme_bw()} and \code{ggplot2::theme_grey()} restore the tick marks
too.
\item The panel titles are facet strips, so they revert to being rotated upright
and centred inside the panel instead of lying flat beside it.
\item The type goes from 9 points to 11, which takes every mark on the chart with
it -- markers about a fifth heavier, and a residual strip a fifth deeper.
}

None of that is an error, and nothing is overridden: a chart is entitled to
be restyled, and \code{+} winning is the whole of \strong{ggplot2}'s contract. But the
chart does say so. It signs its own theme, notices when the signature has
gone, and warns once as it draws:

\if{html}{\out{<div class="sourceCode">}}\preformatted{This chart's theme has been replaced, so some of its design is lost ...
}\if{html}{\out{</div>}}

Only a COMPLETE theme clears the signature. \code{ggplot2::theme(...)} merges, so
ordinary customising never trips it. Adding \code{theme_aev()} after the standard
theme puts the styling back and silences the warning.
}

\examples{
aev <- create_aev(
  A = c(1100, 40, 260),
  E = c(1000, 50, 250),
  V = c(2500, 125, 620)
)
names(aev) <- c("[65, 75)", "[75, 85)", "[85, 95)")

# The same chart, drawn for a larger page.
autoplot(aev) + theme_aev(base_size = 13)
}
\seealso{
\link{aev_plot} for the chart itself.
}
