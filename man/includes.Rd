% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/includes.R
\name{includes}
\alias{includes}
\alias{is_includes}
\alias{group_names}
\alias{group_names.default}
\alias{group_names.includes}
\alias{names<-.includes}
\alias{group_names<-}
\alias{group_names<-.default}
\alias{group_names<-.includes}
\alias{length.includes}
\alias{[.includes}
\alias{[[.includes}
\title{Includes: a collection of includes}
\usage{
includes(...)

is_includes(x)

group_names(x)
}
\arguments{
\item{...}{\code{include} and \code{includes} objects to collect. An \code{includes}
argument is flattened in. A name given to an \code{include} argument replaces
its name; a name given to an \code{includes} argument replaces the group name of
every element it contributes.
An \code{includes} is subsettable with \code{[} and \code{[[}, which take an index and keep
the labels of whatever they select.

Both labels may be replaced. \verb{names<-} takes exactly one name per include;
\verb{group_names<-} takes one per include or a single value for all of them.}

\item{x}{An \code{includes}.}
}
\value{
\code{includes()} returns an \code{includes}.

\code{is_includes()} returns a scalar \code{logical}.

\code{group_names()} returns a \code{character} vector with one element per include.

\verb{names<-()} and \verb{group_names<-()} return a new \code{includes}.
}
\description{
An \code{includes} is an optionally named list of \link{include}s. It is what the
\code{breakdown} argument of an analysis takes, and it is what every plural
constructor -- \code{\link[=bands]{bands()}}, \code{\link[=ages]{ages()}}, \code{\link[=durations]{durations()}}, \code{\link[=periods]{periods()}} -- returns.

It is a separate type rather than an \link{include} with a length, because giving
\code{include} a length would touch every working include path to buy generality
only collections need.

Each element carries \strong{two levels of naming}. Its own \code{names()} entry says
which band it is, and its \code{\link[=group_names]{group_names()}} entry says what was banded. A
fifteen-element breakdown of ages, periods and amounts can then tell a chart
which six are the ages, which a single flat name could not.

Elements need not be disjoint. Examining A/E on intersecting subsets is
legitimate, so \strong{logmu} does not check for overlaps and does not treat a
breakdown as a partition.
}
\examples{
std <- includes(
  ages(65, 95, by = 5),
  periods(2000, 2020, by = 10)
)
std
names(std)
group_names(std)

# Naming an includes argument renames the group of everything in it.
includes(cohort = ages(65, 95, by = 15))
}
