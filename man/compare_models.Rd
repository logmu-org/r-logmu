% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_models.R
\name{compare_models}
\alias{compare_models}
\alias{is_model_comparison}
\alias{best_fit}
\alias{print.model_comparison}
\title{Compare mortality models by penalised log-likelihood}
\usage{
compare_models(
  exp_data,
  models,
  include = NULL,
  weight = NULL,
  val_similarity = NULL,
  val_distance = NULL,
  test_mortality = default_mortality(),
  Z = NULL,
  settings = NULL,
  overdispersion = NULL,
  time_scale = NULL,
  L_tolerance = default_L_tolerance,
  armijo = default_armijo,
  max_iterations = default_max_iterations,
  max_halvings = default_max_halvings,
  threads = cpp_veil_default_threads()
)

is_model_comparison(x)

best_fit(x)

\method{print}{model_comparison}(x, ...)
}
\arguments{
\item{exp_data}{The experience data.}

\item{models}{A list of \code{\link[=model]{model()}}s, ideally named. Names appear in the
ranking and index \code{fits}; unnamed candidates are labelled by position.}

\item{include}{An \code{include} restricting the population, or \code{NULL} for all of
it.}

\item{weight}{A pronoun expression for the weight \eqn{w}, or \code{NULL} for
lives. Weights must not be negative.}

\item{val_similarity, val_distance}{A pronoun expression for the second
weighting factor, in either spelling. At most one may be given.}

\item{test_mortality}{The mortality on which the \eqn{Z} scale is measured,
once, for every candidate. See \code{\link[=fit]{fit()}}.}

\item{Z}{The \eqn{Z} scale as a single positive number, given instead
of a \code{test_mortality} rather than as well as one.}

\item{settings}{A \code{\link[=settings]{settings()}} object carrying \code{overdispersion} and
\code{time_scale}.}

\item{overdispersion, time_scale}{Given directly, these override the
\code{settings}.}

\item{L_tolerance}{The convergence tolerance, as a gain in \eqn{L}.}

\item{armijo}{The fraction of the linearly predicted gain a damped step must
actually deliver to be accepted.}

\item{max_iterations}{The most Newton steps to take.}

\item{max_halvings}{The most times one step may be halved before a candidate
is abandoned.}

\item{threads}{The number of worker threads.}

\item{x}{A \code{model_comparison}.}

\item{...}{Ignored.}
}
\value{
\code{compare_models()} returns a \code{model_comparison}, whose fields are \code{table} (the
ranking, best first, with columns \code{k}, \code{log_likelihood}, \code{penalty},
\code{penalised_log_likelihood} and \code{status}), \code{fits} (the \code{fit} of each candidate,
in the same order, \code{NULL} where one failed), \code{failures} (the diagnosis of each
failure, named, empty where there were none), \code{Z}, \code{overdispersion},
\code{diagnostics} (the population and test-mortality numbers the header reports)
and \code{labels} (what was written for the include, the weight and the test
mortality).

\code{table} holds the values as computed. \code{print()} takes the best candidate's
\eqn{L_{\mathrm{P}}} off both likelihood columns, because differences are what
matter and the absolute numbers are large and uninformative -- so the winner
prints as 0. Both columns shift by the same constant, which keeps
\eqn{L - L_{\mathrm{P}} = p} true row by row.

\code{is_model_comparison()} returns a scalar \code{logical}.

\code{best_fit()} returns the \code{fit} of the highest-ranked candidate.
}
\description{
Fits several candidate \code{\link[=model]{model()}}s against one set of experience data and
ranks them on penalised log-likelihood,
\deqn{L_{\mathrm{P}} = L(\hat\beta) - p, \qquad p = Z^{-1}\mathrm{tr}(\mathbf{J}\mathbf{I}^{-1}).}
Larger is better. This is Akaike's information criterion without the factor
of \eqn{-2}, generalised to an arbitrary log-likelihood weight.
}
\section{What makes the candidates comparable}{
Every candidate is fitted over the same data, \code{include}, weight,
overdispersion and \eqn{Z}, because all of those are arguments of this call
and none of them is part of a \code{model}. \eqn{Z} in particular is resolved once
and shared: it is measured on \code{test_mortality} over this data one time, not
once per candidate.

Candidates may differ in their reference mortality. The constant
\eqn{\mathrm{A}w\log\mu^{\mathrm{ref}}} is retained in \eqn{L}, so a
comparison across different base tables is meaningful.
}

\section{Reading the shortfall}{
Where the weight is absent or is an indicator, \eqn{w^2 = w} makes \eqn{p}
exactly \code{k}, \eqn{L_{\mathrm{P}}} exactly \eqn{-}AIC\eqn{/2}, and a shortfall
of 1 one parameter's worth -- which is the usual threshold for a difference
that matters. For an \emph{ad hoc} weight the penalty is no longer a count and the
shortfall carries no such calibration; it still ranks the candidates, but how
large a shortfall is worth acting on is a question this function does not
answer.

Overdispersion does real work in the ranking. The penalty is free of
\eqn{\Omega} and the fit term is not, so a larger \eqn{\Omega} favours
simpler models, which is the quasi-likelihood behaviour and is why
\code{overdispersion} is yours to supply.
}

\examples{
data <- exp_data(
  list(
    birth     = datey::datey(c(1945, 1950, 1955, 1940, 1948)),
    male      = c(TRUE, FALSE, TRUE, FALSE, TRUE),
    E2R_start = datey::datey(rep(2015, 5)),
    E2R_end   = datey::datey(c(2020, 2020, 2018, 2020, 2019)),
    E2R_died  = c(FALSE, TRUE, TRUE, FALSE, FALSE)
  ),
  exp_start = datey::datey(2015),
  exp_end   = datey::datey(2020)
)

# One death of each sex, so the split is identifiable.
reference <- gompertz_mortality()
comparison <- compare_models(
  data,
  models = list(
    level = model(reference, covariates(level = 1)),
    sex   = model(reference, covariates(male = .i$male, female = !.i$male))
  ),
  settings = settings(overdispersion = 1)
)
comparison
coef(best_fit(comparison))
}
