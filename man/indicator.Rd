% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator.R
\name{indicator}
\alias{indicator}
\alias{is_indicator}
\alias{logical_value}
\alias{logical_value.default}
\alias{logical_value.indicator}
\title{Indicators: a 0/1 function of an individual}
\usage{
indicator(expr)

is_indicator(x)

logical_value(x, .i)

\method{logical_value}{default}(x, .i)

\method{logical_value}{indicator}(x, .i)
}
\arguments{
\item{expr}{A pronoun expression, optionally written as a \code{~} formula.}

\item{x}{An \code{indicator}.}

\item{.i}{A named list of scalar facts.}
}
\value{
\code{indicator()} returns an \code{indicator}.

\code{is_indicator()} returns a scalar \code{logical}.

\code{logical_value()} returns a \code{logical}.
}
\description{
An \code{indicator} is a time-invariant function of an individual's facts that
takes the value 0 or 1. It is the one type that is both a \code{variable} (a
\eqn{\{0,1\}} weight, for which \eqn{E = V}) and an \code{include} (a subset:
TRUE selects all of time, FALSE selects nothing).

Build one from a pronoun expression or a \code{~} formula. The expression must
not use time \code{.t}. A logical result is always a valid indicator; a numeric
result is accepted but is only checked to be \eqn{\{0,1\}} when evaluated.

\code{logical_value()} evaluates the indicator against an individual's facts \code{.i}
and returns a \code{logical}. Because an indicator is also an \code{include},
\code{period_included()} works too, returning all-of-time or the empty interval.
}
\examples{
male <- indicator(.i$sex == "male")
logical_value(male, .i = list(sex = "male"))
period_included(male, .i = list(sex = "female"))   # empty interval
}
