% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/category.R
\name{category}
\alias{category}
\alias{categories}
\title{Categories: breaking a population down by a categorical field}
\usage{
category(variable, ..., .source = NULL, .known = !is.null(.source))

categories(
  variable,
  ...,
  .source = NULL,
  .known = !is.null(.source),
  .exhaustive = !is.null(.source),
  .disjoint = TRUE
)
}
\arguments{
\item{variable}{A pronoun expression naming the field to divide, e.g.
\code{.i$status}. It must not depend on time \code{.t}.}

\item{...}{The groups. Each argument is one group; a character vector of
several values is one group joining them. A name labels the group.}

\item{.source}{Where the field's values come from -- a factor, a character
vector or column, or a dataset holding a column of the field's name. \code{NULL}
for none. Named with a dot because every other name belongs to \code{...}.}

\item{.known}{Whether every category in \code{...} must be a value of \code{.source}.}

\item{.exhaustive}{Whether every value of \code{.source} must fall in some group.}

\item{.disjoint}{Whether no value may fall in more than one group.}
}
\value{
\code{category()} returns an \code{indicator}, which is also an \code{include}.

\code{categories()} returns an \code{\link[=includes]{includes()}}.
}
\description{
\code{category()} and \code{categories()} build \link{include}s from the values of a
categorical field such as a scheme, a status or a sex. They complete the
constructor family: \code{\link[=bands]{bands()}} cuts a continuous variable, \code{categories()}
divides a discrete one.

Each argument in \code{...} is \strong{one group}, so a character vector joins its
values into a single group rather than splitting into several. Names label
the groups.

\code{.source} says where the field's values come from. With no \code{...} it supplies
the groups, one per value; with \code{...} it is what the written categories are
checked against.

Every group is an \link{indicator}, so \eqn{V = \Omega E} holds within each.
}
\section{Where the values come from}{

\code{.source} may be:
\itemize{
\item a \strong{factor}, giving its declared \code{levels()} in their declared order,
including any level no record uses;
\item a \strong{character} vector or column, giving its distinct values in
alphabetical order;
\item a \strong{dataset} (\code{exp_data}, \code{val_data} or a data frame), giving the column
named by the field, which requires \code{variable} to be a plain field.
}

Prefer a factor when the breakdown is to be reused across several datasets.
A factor's levels are a property of the column rather than of its contents,
so every dataset yields the same groups in the same order and the results
stay comparable. Two character columns need not.
}

\section{Checking}{

Three checks guard the grouping, and each runs whenever it has what it
needs:
\itemize{
\item \code{.known} -- every category written in \code{...} is a value of \code{.source}. This
catches a mistyped category.
\item \code{.exhaustive} -- every value of \code{.source} falls in some group. This catches
a value added to the data later, which would otherwise be dropped from the
breakdown in silence.
\item \code{.disjoint} -- no value falls in more than one group.
}

The first two need \code{.source} and so default on when one is given; the third
needs only \code{...} and so is always on. Setting \code{.known} or \code{.exhaustive}
without a \code{.source} is an error, since there would be nothing to check
against.

\code{.disjoint} is not the route to overlapping groups. Build those separately
and collect them with \code{\link[=includes]{includes()}}, which checks nothing across its
elements.
}

\examples{
# One group per status, written out.
categories(.i$status, "act", "def", "pen")

# A join, and named groups.
categories(.i$status, active = "act", other = c("def", "dep"))

# Every level of a factor, in its declared order.
scheme <- factor(c("A", "B", "A"), levels = c("A", "B", "C"))
categories(.i$scheme, .source = scheme)

# A single group of two joined values.
category(.i$status, c("def", "dep"))
}
