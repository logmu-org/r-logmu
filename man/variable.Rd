% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable.R
\name{variable}
\alias{variable}
\alias{static_variable}
\alias{is_variable}
\alias{is_static_variable}
\alias{value}
\alias{value.default}
\alias{value.variable}
\title{Variables: scalar functions of an individual and time}
\usage{
variable(expr)

static_variable(expr)

is_variable(x)

is_static_variable(x)

value(x, .i, .t = NULL, ...)

\method{value}{default}(x, .i, .t = NULL, ...)

\method{value}{variable}(x, .i, .t = NULL, ...)
}
\arguments{
\item{expr}{A pronoun expression, optionally written as a \code{~} formula.}

\item{x}{A \code{variable}.}

\item{.i}{A named list of scalar facts.}

\item{.t}{A vector of \code{datey} (omit for a time-invariant variable).}

\item{...}{Unused.}
}
\value{
\code{variable()} and \code{static_variable()} return a \code{variable} (the latter also
classed \code{static_variable}).

\code{is_variable()} and \code{is_static_variable()} return a scalar \code{logical}.

\code{value()} returns a vector aligned to \code{.t}.
}
\description{
A \code{variable} is a scalar, real-valued function \eqn{f(i, t)} written with
the data pronouns \code{.i$field}, \code{.t}, \code{.b} and \code{.x}. A \code{static_variable} is a
variable that does not depend on time -- a function of the individual's
facts alone, \eqn{f(i)}.

Construct one from a pronoun expression or a \code{~} formula. The constructors
return the \strong{most specific} type they can prove: a time-invariant
expression becomes a \code{static_variable}, and one that is also structurally
logical becomes an \code{indicator}. \code{static_variable()} additionally asserts that
the expression does not use \code{.t} (and fails if it does); \code{indicator()}
asserts a \eqn{\{0,1\}} value. So \code{variable(.i$pension)} is a
\code{static_variable} and \code{variable(.i$pension > 0)} is an \code{indicator}.

\code{value()} evaluates the variable against a single individual's facts \code{.i}
(a named list) and a time vector \code{.t}. It is the plain-R reference path for
testing and understanding -- not the performance path. For a
\code{static_variable}, \code{.t} may be omitted.
}
\examples{
amounts <- static_variable(.i$pension)
value(amounts, .i = list(pension = 1000))

v <- variable(.i$pension * 2)
value(v, .i = list(pension = 1000))
}
