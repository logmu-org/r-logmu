# Get started

``` r

library(logmu)
```

## Installation

``` r

# Install the released version from CRAN
install.packages("logmu")

# Or the development version from GitHub:
# install.packages("pak")
pak::pak("logmu-org/r-logmu")
```

## Example

``` r

library(logmu)
```

TODO
